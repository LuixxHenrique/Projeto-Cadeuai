import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class MapGenerationService {

  constructor() { }

  // image_set_css(image: HTMLElement, scale: number){
  //   const scaleString = `scale(${scale})`
  //   const rotate = "rotate(-90deg)"
  //   image.style.transform = `${scaleString} ${rotate}`
  //   // image.style.margin = 
  // }
}
